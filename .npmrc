
# The sidecar deploy uses --config.node-linker=hoisted + --prod, which leaves
# the workspace deps status "out of sync"; pnpm 11 would then auto-run
# `install --production` before every `pnpm run`, purging dev deps. Disable
# the check so scripts never trigger that destructive reinstall.
verify-deps-before-run=false
